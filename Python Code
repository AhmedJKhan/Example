#Wordclouds are a powerfull tool to describe the text data. They are specially helpful in assessment of customer feedback, behaviors and SEO words selection.
#Creating Wordclouds is simple using Python. and described step by step below;
!pip install wordcloud
!pip install fileupload
!pip install ipywidgets
!jupyter nbextension install --py --user fileupload
!jupyter nbextension enable --py fileupload

import wordcloud
import numpy as np
from matplotlib import pyplot as plt
from IPython.display import display
import fileupload
import io
import sys
